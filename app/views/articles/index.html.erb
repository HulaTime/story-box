<% @articles.each do |article| %>
	<% article_lines = article.body.split("\n")[0..2].join("\n") %>
	<p> <%=link_to article.title, article_path(article) %> </p>
	<p> <%= article_lines %> </p>
<% end %>

<% if user_signed_in? %>
	<%= link_to 'New Article', new_article_path %>
<% end %>