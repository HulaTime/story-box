<div class='sticky-notes'>
	<ul>
		<% @notes.each do |note| %>
			<!-- <% note_num = 1 %> -->
			<li>
				<p>
					<%= note.body %>
				</>
			</li>
			<!-- <% note_num += 1 %> -->
		<% end %>
	</ul>
</div>

<%= link_to 'New Note', new_note_path %>